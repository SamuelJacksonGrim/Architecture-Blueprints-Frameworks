# System Flow (template)

> Behavioral view: the primary request/execution path end to end.

```
Input → Validate → Process → Decide → Act → Output
                       │
                       └── (error) → Handle → Output
```
