# QUALITY-BAR — rigor of the *result*

Consult this when marking an artifact `complete` and once at the end.
It is not a gate between thoughts and not part of the SELECTOR load list.

---

## 1. Specificity over boilerplate
A `complete` artifact describes *this* system — real names, numbers, thresholds.
If it would read the same for a different system, it isn't done.

## 2. Name the invariants that look optional but aren't
Subtle breakage, not loud errors. Each with *why* and the failure it prevents.

## 3. Guardrails name the prohibition and the legitimate alternative

## 4. One authority per kind of decision
Everyone else produces input.

## 5. Constants carry provenance
Derived values name their source. A validator must not depend on what it certifies.

## 6. Observe-only paths stay one-way

## 7. DecisionLog records why, titled as the question
Append-only. Continuous and selective — scope in `SCHEMA.md`.
A log of every thought is a failed log.

## 8. Docs match reality in the same change

## 9. Prove the main path, or disclose that you could not

---

Behaviorally significant decisions belong in the persisted design. Glue does not.
A longer transcript is not more rigor.
